# Repair Hints

Repair generated tools by using declared resource selectors, keeping `run(arguments: dict, resources: dict) -> dict`, compiling `tool.py`, and validating through the real ToolCompiler/Gateway binding path.
