# Repair Hints

Create or repair `agent_package.json` first when package loading fails. Keep every path package-relative, create the referenced file before final validation, and route contract-specific repairs to the relevant contract skill instead of expanding the manifest format.
