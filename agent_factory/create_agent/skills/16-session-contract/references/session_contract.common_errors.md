# Common Errors

- Omitting the session contract while `AgentPackageManifest` requires it.
- Pointing checkpoint paths outside the runtime sandbox.
- Replacing the contract with ad hoc package files instead of using `session_contract.v0`.
