# Repair Hints

Create `contracts/session.json` from `examples/session_contract.minimal.json` first. Only change the checkpoint backend or paths when the current todo explicitly requires a different session persistence policy.
