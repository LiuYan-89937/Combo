# Repair Hints

Keep package nodes deterministic, package-relative, and explicit about state reads/writes. If logic is user-callable, prefer a tool; if logic is model reasoning, prefer a cognitive node.
