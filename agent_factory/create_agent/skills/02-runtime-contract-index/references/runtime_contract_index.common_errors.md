# Common Errors

- Adding a contract because it sounds useful rather than because the package uses it.
- Centralizing all contract schemas in the index skill.
- Inventing a contract key that the runtime registry does not know how to build.
