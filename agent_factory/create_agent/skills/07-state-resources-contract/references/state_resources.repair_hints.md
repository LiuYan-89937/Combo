# Repair Hints

State is graph-owned data; resources are runtime configuration and external handles. Required resources that cannot be discovered safely must become natural-language user questions through `create_agent_control`.
