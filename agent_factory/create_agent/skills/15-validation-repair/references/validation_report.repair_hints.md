# Repair Hints

Treat validator output as authoritative. Load the recommended skill, read the recommended resources, repair the target files, then run `create_agent_validate` or finalize through `create_agent_control`.
