# Combo 角色化 UI 图标

此目录的 `chroma/` 保存角色化 PNG 去背源稿，包括计划、权限、发送、上下文，以及空会话、空记忆和空工作区。

产品使用的透明 PNG 位于 `web_frontend/frontend/public/brand/combo/ui-icons/`，统一为 512 × 512 RGBA。交互状态通过 CSS 反色、透明度、位移和旋转表达，不派生 SVG 图标。
