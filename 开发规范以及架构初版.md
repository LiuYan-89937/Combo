# AgentFactory Framework 开发骨架与设计规范

> 版本：v0.1
> 定位：一个以 `AgentFactory` 为核心的 **声明式 Agent 生成、运行、测试、隔离、热更新、返厂升级框架**。
> 第一阶段实现语言：Python
> 第一阶段核心目标：框架抽象与流程闭环，不追求完整产品化后台。
> 第一批目标用户：非技术人员，通过自然语言描述需求创建和升级 Agent；后续提供封装函数与底层 SDK，支持开发者二次开放。

---

## 1. 项目定位

本项目不是一个简单的 Agent 创建工具，也不是单纯的 Tool Calling 封装库，而是一个类似 LangChain / LangGraph 思想、但更偏向 **Agent 制造与生命周期管理** 的框架。

核心思想：

```text
用户完整需求
  ↓
AgentFactoryAgent 理解需求
  ↓
生成声明式 AgentPackage
  ↓
生成 Harness 测试工程
  ↓
注册能力、工具、上下文、MCP
  ↓
创建强隔离 AgentInstance
  ↓
图 Runtime 执行
  ↓
运行中发现新需求 / 能力缺失 / 失败模式
  ↓
生成 UpgradeRequest
  ↓
返厂升级
  ↓
测试、版本化、热更新、回滚
```

一句话定义：

> AgentFactory Framework 是一个能够根据自然语言需求制造、测试、注册、运行和持续升级 Agent 的声明式框架。

第一阶段默认交互方式：

```text
非技术使用者用自然语言描述目标
  ↓
AgentFactoryAgent 生成 AgentPackage 草稿
  ↓
使用者确认关键能力、工具、权限和风险
  ↓
Harness 可复现测试
  ↓
注册、运行、升级
```

因此，`AgentPackage` 是框架内部的标准工程中间表示，不要求第一批用户直接手写 YAML。

---

## 2. 第一阶段边界

### 2.1 已确定设计选择

| 问题           | 第一阶段选择                              |
| ------------ | ----------------------------------- |
| Agent 生成方式   | 方案 C：自然语言生成 AgentPackage 草稿，使用者确认后进入 Harness |
| 工具代码生成       | 允许 Factory 生成工具实现代码，但必须经过静态检查、沙箱测试、Harness 和审批 |
| LLM 交互边界       | 核心层统一通过 ModelService 调用模型，不直接 HTTP，不让 LLM 直接执行工具 |
| Agent 标准件       | Instruction / Output / Conversation / Toolset / Knowledge / Guardrail / Handoff / Observability 为核心必需结构 |
| Runtime 执行模型 | 固定流程使用 WorkflowRuntime，复杂多变场景使用 GraphRuntime |
| 条件表达式         | 使用受限 Python 表达式                       |
| 返厂升级触发       | 用户主动、Agent 自检、系统监控三种都支持             |
| 热更新边界        | 不涉及系统安全的能力可立即更新，高风险能力需要审核 / 测试 / 灰度 |
| 隔离方式         | 强隔离，不同 AgentInstance 独立运行           |
| MCP             | 第一阶段必须实现，作为外部 Capability Source 接入标准 MCP 协议 |
| Registry 存储    | 第一版使用文件系统，后续抽象为可替换存储能力              |
| 实现语言         | Python                              |
| 当前目标         | 框架，不是完整产品                           |

### 2.2 第一阶段做什么

第一阶段重点实现：

```text
1. AgentPackage 声明式规范
2. Model / LLM 统一交互层
3. Agent Building Primitives 标准件
4. Capability 统一抽象
5. Tool / MCP / Context / Memory / Harness 标准格式
6. Runtime 执行模型抽象
7. AgentFactoryAgent 的流程抽象
8. AgentInstance 强隔离运行模型
9. Harness 准入测试流程
10. UpgradeRequest 返厂升级协议
11. Registry 注册中心抽象
12. 热更新与版本切换协议
13. 自然语言到 AgentPackage 草稿的方案 C 流程
14. MCP 标准协议接入与 MCP Capability 映射
15. 可复现、可追踪的 Harness 报告
16. 文件系统 Registry 与版本历史
17. PatchPlan 返厂升级计划协议
```

### 2.3 第一阶段不做什么

第一阶段暂不重点实现：

```text
1. 完整可视化后台
2. 复杂企业权限系统
3. 多租户商业化管理
4. 大规模分布式调度
5. 未经静态检查、沙箱测试、Harness 和审批的任意 Python 业务代码上线
6. 真正生产级容器编排平台
7. 完整模型训练 / 微调系统
```

---

## 3. 核心概念

### 3.1 AgentFactoryAgent

`AgentFactoryAgent` 是框架中的元 Agent。

它不是直接服务终端用户，而是负责制造和维护其他 Agent。

职责：

```text
1. 理解非技术使用者的完整需求
2. 拆解 Agent 能力边界
3. 生成 AgentBlueprint
4. 生成 AgentPackage 草稿
5. 生成工具、上下文、MCP、Memory、Workflow 声明
6. 在必要时生成工具实现代码草稿
7. 生成 Harness 测试规范
8. 等待使用者确认关键能力、权限、风险和高影响变更
9. 调用 HarnessRunner 验证 AgentPackage
10. 注册 AgentPackage
11. 创建 AgentInstance
12. 接收 UpgradeRequest 并进行返厂升级
```

### 3.2 AgentBlueprint

`AgentBlueprint` 是设计图，位于自然语言需求和 AgentPackage 之间。

它描述：

```text
这个 Agent 是什么？
面向什么场景？
有哪些意图？
有哪些工具？
有哪些上下文？
有哪些 MCP？
有哪些工作流节点？
有哪些安全策略？
如何测试？
如何升级？
```

### 3.3 AgentPackage

`AgentPackage` 是 Agent 的标准工程包，是框架真正加载、测试、注册、运行的对象。

一个 AgentPackage 必须包含：

```text
agent.yaml        Agent 基本信息与总入口
context.yaml      上下文管理规范
tools.yaml        工具能力声明
mcp.yaml          MCP 接入声明
workflow.yaml     图结构交互逻辑
memory.yaml       记忆策略
policy.yaml       权限与风控策略
harness.yaml      测试准入规范
runtime.yaml      运行时配置
version.yaml      版本与升级信息
```

### 3.4 AgentInstance

`AgentInstance` 是某个 AgentPackage 的运行实例。

同一个 AgentPackage 可以创建多个实例。

例如：

```text
customer-service-agent:v1
  ├── instance: cs-agent-prod-001
  ├── instance: cs-agent-canary-001
  └── instance: cs-agent-test-001
```

实例之间必须隔离：

```text
上下文隔离
记忆隔离
工具权限隔离
MCP 连接隔离
运行进程隔离
日志追踪隔离
版本隔离
```

### 3.5 Capability

所有可插拔能力统一抽象成 `Capability`。

```text
Capability
├── ToolCapability
├── MCPCapability
├── ContextCapability
├── MemoryCapability
├── RetrieverCapability
├── WorkflowCapability
├── IntentCapability
├── GuardrailCapability
├── EvalCapability
└── ModelCapability
```

这样工具、上下文、MCP、记忆、测试、模型都可以进入统一注册、测试、版本管理和热更新流程。

`Capability` 是顶层抽象，不直接承载所有领域细节。不同能力领域需要继承该抽象，并根据领域特性添加字段。

例如：

```text
ToolCapability
  ├── input_schema
  ├── output_schema
  ├── risk_level
  ├── side_effects
  └── approval_policy

MCPCapability
  ├── server_ref
  ├── mcp_method
  ├── input_mapping
  ├── output_mapping
  └── trust_policy

MemoryCapability
  ├── namespace_pattern
  ├── ttl
  ├── read_policy
  ├── write_policy
  └── isolation_policy
```

### 3.6 Harness

`Harness` 是 AgentPackage 的强制准入测试工程。

任何 AgentPackage 生成后，必须同时生成 Harness。

Harness 不只是测工具是否可调用，还要测：

```text
意图识别是否正确
上下文是否正确加载
工具是否正确选择
MCP 是否连接稳定
图流程是否正确跳转
危险请求是否拦截
升级触发是否合理
历史能力是否回归通过
```

### 3.7 UpgradeRequest

`UpgradeRequest` 是返厂升级协议。

AgentInstance 不能私自修改自己，只能发起升级请求。

真正升级必须经过：

```text
UpgradeRequest
  ↓
AgentFactoryAgent 分析
  ↓
生成 PatchPlan
  ↓
校验 PatchPlan
  ↓
高风险 change 逐项审批
  ↓
生成 ApprovalRecord
  ↓
PatchPlanExecutor 修改 AgentPackage
  ↓
生成 PackageDiff
  ↓
校验 PackageDiff
  ↓
生成新版本
  ↓
Harness 测试
  ↓
注册新版本
  ↓
热更新 / 灰度 / 回滚
```

---

## 4. 总体架构

```text
agent_factory_framework/

┌─────────────────────────────────────────────────────┐
│                    Factory Plane                    │
│                                                     │
│  AgentFactoryAgent                                  │
│  ├── RequirementAnalyzer                            │
│  ├── AgentDesigner                                  │
│  ├── PackageGenerator                               │
│  ├── HarnessGenerator                               │
│  ├── UpgradePlanner                                 │
│  └── ReleasePlanner                                 │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│                    Runtime Plane                    │
│                                                     │
│  AgentRuntime                                       │
│  ├── RuntimeSelector                                │
│  ├── WorkflowRuntime                                │
│  ├── GraphRuntime                                   │
│  ├── StateManager                                   │
│  ├── ContextManager                                 │
│  ├── ToolRouter                                     │
│  ├── MCPClientManager                               │
│  ├── MemoryManager                                  │
│  ├── CheckpointManager                              │
│  └── UpgradeDetector                                │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│                  Capability Plane                   │
│                                                     │
│  Registry System                                    │
│  ├── AgentRegistry                                  │
│  ├── CapabilityRegistry                             │
│  ├── ToolRegistry                                   │
│  ├── MCPRegistry                                    │
│  ├── ContextRegistry                                │
│  ├── MemoryRegistry                                 │
│  ├── ModelRegistry                                  │
│  └── PolicyRegistry                                 │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│                       Ops Plane                     │
│                                                     │
│  AgentOps                                           │
│  ├── HarnessRunner                                  │
│  ├── EvalRunner                                     │
│  ├── TraceLogger                                    │
│  ├── MetricsCollector                               │
│  ├── VersionManager                                 │
│  ├── ReleaseManager                                 │
│  ├── RollbackManager                                │
│  └── IsolationManager                               │
└─────────────────────────────────────────────────────┘
```

---

## 5. 推荐 Python 项目目录

```text
agent_factory_framework/
├── README.md
├── pyproject.toml
├── configs/
│   ├── framework.yaml
│   └── registry.yaml
│
├── agent_factory/
│   ├── __init__.py
│   │
│   ├── core/
│   │   ├── __init__.py
│   │   ├── types.py
│   │   ├── errors.py
│   │   ├── constants.py
│   │   └── events.py
│   │
│   ├── specs/
│   │   ├── __init__.py
│   │   ├── agent_spec.py
│   │   ├── context_spec.py
│   │   ├── tool_spec.py
│   │   ├── mcp_spec.py
│   │   ├── workflow_spec.py
│   │   ├── memory_spec.py
│   │   ├── policy_spec.py
│   │   ├── harness_spec.py
│   │   ├── runtime_spec.py
│   │   ├── version_spec.py
│   │   ├── upgrade_spec.py
│   │   └── patch_plan_spec.py
│   │
│   ├── factory/
│   │   ├── __init__.py
│   │   ├── factory_agent.py
│   │   ├── requirement_analyzer.py
│   │   ├── blueprint_generator.py
│   │   ├── package_generator.py
│   │   ├── harness_generator.py
│   │   ├── upgrade_planner.py
│   │   └── release_planner.py
│   │
│   ├── package/
│   │   ├── __init__.py
│   │   ├── package_loader.py
│   │   ├── package_validator.py
│   │   ├── package_writer.py
│   │   ├── package_diff.py
│   │   ├── patch_plan.py
│   │   ├── patch_plan_executor.py
│   │   ├── approval_record.py
│   │   ├── approval_validator.py
│   │   ├── package_diff_validator.py
│   │   └── package_schema.py
│   │
│   ├── registry/
│   │   ├── __init__.py
│   │   ├── base_registry.py
│   │   ├── agent_registry.py
│   │   ├── capability_registry.py
│   │   ├── tool_registry.py
│   │   ├── mcp_registry.py
│   │   ├── context_registry.py
│   │   ├── memory_registry.py
│   │   ├── model_registry.py
│   │   └── policy_registry.py
│   │
│   ├── capability/
│   │   ├── __init__.py
│   │   ├── base.py
│   │   ├── tool_capability.py
│   │   ├── mcp_capability.py
│   │   ├── context_capability.py
│   │   ├── memory_capability.py
│   │   ├── retriever_capability.py
│   │   ├── workflow_capability.py
│   │   ├── guardrail_capability.py
│   │   └── eval_capability.py
│   │
│   ├── runtime/
│   │   ├── __init__.py
│   │   ├── agent_runtime.py
│   │   ├── graph_runtime.py
│   │   ├── node.py
│   │   ├── edge.py
│   │   ├── state.py
│   │   ├── event_bus.py
│   │   ├── checkpoint.py
│   │   ├── interrupt.py
│   │   └── upgrade_detector.py
│   │
│   ├── context/
│   │   ├── __init__.py
│   │   ├── context_manager.py
│   │   ├── context_loader.py
│   │   ├── context_selector.py
│   │   ├── context_compiler.py
│   │   ├── context_compressor.py
│   │   └── context_visibility.py
│   │
│   ├── tools/
│   │   ├── __init__.py
│   │   ├── base_tool.py
│   │   ├── tool_router.py
│   │   ├── tool_executor.py
│   │   ├── tool_adapter.py
│   │   ├── tool_schema.py
│   │   └── tool_result.py
│   │
│   ├── mcp/
│   │   ├── __init__.py
│   │   ├── mcp_client.py
│   │   ├── mcp_manager.py
│   │   ├── mcp_binding.py
│   │   ├── mcp_health.py
│   │   └── mcp_adapter.py
│   │
│   ├── memory/
│   │   ├── __init__.py
│   │   ├── memory_manager.py
│   │   ├── session_memory.py
│   │   ├── task_memory.py
│   │   ├── user_memory.py
│   │   └── memory_policy.py
│   │
│   ├── harness/
│   │   ├── __init__.py
│   │   ├── harness_runner.py
│   │   ├── test_case.py
│   │   ├── intent_harness.py
│   │   ├── tool_harness.py
│   │   ├── mcp_harness.py
│   │   ├── context_harness.py
│   │   ├── workflow_harness.py
│   │   ├── safety_harness.py
│   │   ├── regression_harness.py
│   │   └── report.py
│   │
│   ├── ops/
│   │   ├── __init__.py
│   │   ├── trace_logger.py
│   │   ├── metrics.py
│   │   ├── version_manager.py
│   │   ├── release_manager.py
│   │   ├── rollback_manager.py
│   │   ├── isolation_manager.py
│   │   └── sandbox_manager.py
│   │
│   ├── isolation/
│   │   ├── __init__.py
│   │   ├── instance_manager.py
│   │   ├── process_runtime.py
│   │   ├── container_runtime.py
│   │   ├── namespace.py
│   │   └── resource_limit.py
│   │
│   └── cli/
│       ├── __init__.py
│       ├── main.py
│       ├── create_agent.py
│       ├── run_agent.py
│       ├── test_agent.py
│       ├── upgrade_agent.py
│       ├── plan_upgrade.py
│       ├── approve_patch.py
│       ├── apply_patch_plan.py
│       ├── approval_cmd.py
│       ├── diff_cmd.py
│       └── registry_cmd.py
│
├── examples/
│   ├── customer_service_agent/
│   │   ├── agent.yaml
│   │   ├── context.yaml
│   │   ├── tools.yaml
│   │   ├── mcp.yaml
│   │   ├── workflow.yaml
│   │   ├── memory.yaml
│   │   ├── policy.yaml
│   │   ├── harness.yaml
│   │   ├── runtime.yaml
│   │   └── version.yaml
│   │
│   └── code_review_agent/
│       └── ...
│
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── harness_cases/
│   └── fixtures/
│
└── docs/
    ├── architecture.md
    ├── agent_package_spec.md
    ├── workflow_graph_spec.md
    ├── tool_spec.md
    ├── mcp_spec.md
    ├── harness_spec.md
    ├── upgrade_protocol.md
    └── isolation_model.md
```

---

## 6. AgentPackage 标准结构

一个 AgentPackage 是一个目录。

```text
customer-service-agent/
├── agent.yaml
├── context.yaml
├── tools.yaml
├── mcp.yaml
├── workflow.yaml
├── memory.yaml
├── policy.yaml
├── harness.yaml
├── runtime.yaml
├── version.yaml
├── generated/
│   └── draft_tools/
└── patches/
    └── draft_patch_plans/
```

### 6.1 必须文件

| 文件            | 是否必须 | 作用               |
| ------------- | ---- | ---------------- |
| agent.yaml    | 必须   | Agent 总定义        |
| workflow.yaml | 必须   | 图执行逻辑            |
| context.yaml  | 必须   | 上下文规则            |
| tools.yaml    | 必须   | 工具声明，即使为空也必须存在   |
| mcp.yaml      | 必须   | MCP 声明，即使为空也必须存在 |
| memory.yaml   | 必须   | 记忆策略             |
| policy.yaml   | 必须   | 权限、安全、热更新边界      |
| harness.yaml  | 必须   | 测试准入规范           |
| runtime.yaml  | 必须   | 运行时和隔离配置         |
| version.yaml  | 必须   | 版本、兼容性、升级信息      |
| generated/    | 可选   | Factory 生成代码草稿区    |
| patches/      | 可选   | PatchPlan 与补丁草稿区   |

### 6.2 设计原则

```text
1. 所有 Agent 都必须以 AgentPackage 形式存在。
2. 所有 Package 都必须可校验。
3. 所有 Package 都必须可测试。
4. 所有 Package 都必须版本化。
5. 所有 Package 都必须声明自己的工具、MCP、上下文和记忆边界。
6. AgentInstance 不直接修改 Package，只能通过 UpgradeRequest 生成新版本。
7. Factory 生成的工具代码必须先进入 generated/ 草稿区，审批通过后才能进入正式工具实现目录。
8. PatchPlan 必须先进入 patches/ 草稿区，校验和审批通过后才能执行。
```

---

## 7. agent.yaml 规范

### 7.1 作用

`agent.yaml` 是 AgentPackage 的入口文件。

它描述：

```text
Agent 名称
Agent 类型
业务目标
输入输出规范
绑定的 workflow/context/tools/mcp/memory/policy/harness 文件
```

### 7.2 示例

```yaml
schema_version: "0.1"
kind: "Agent"

metadata:
  name: "customer-service-agent"
  display_name: "客服机器人"
  version: "1.0.0"
  owner: "agent-factory"
  description: "处理售前、售后、退款、返厂维修、投诉与人工转接的客服 Agent。"

agent:
  type: "graph_agent"
  domain: "customer_service"
  goal: "在多轮对话中识别用户服务意图，调用合适工具，维护服务上下文，并在能力不足时触发返厂升级。"
  language: "zh-CN"

entrypoints:
  main_workflow: "workflow.yaml"
  context_spec: "context.yaml"
  tool_spec: "tools.yaml"
  mcp_spec: "mcp.yaml"
  memory_spec: "memory.yaml"
  policy_spec: "policy.yaml"
  harness_spec: "harness.yaml"
  runtime_spec: "runtime.yaml"

io:
  input:
    type: "message"
    fields:
      - name: "user_input"
        type: "string"
        required: true
      - name: "user_id"
        type: "string"
        required: false
      - name: "session_id"
        type: "string"
        required: false
  output:
    type: "agent_response"
    fields:
      - name: "content"
        type: "string"
      - name: "intent"
        type: "string"
      - name: "tool_calls"
        type: "array"
      - name: "upgrade_request"
        type: "object"
        required: false

required_capabilities:
  - "intent_detection"
  - "context_management"
  - "tool_routing"
  - "mcp_binding"
  - "memory_management"
  - "upgrade_detection"

lifecycle:
  create_by: "AgentFactoryAgent"
  allow_refactory: true
  allow_hot_update: true
  require_harness_before_release: true
```

### 7.3 规范要求

```text
1. metadata.name 必须全局唯一。
2. metadata.version 必须符合语义化版本。
3. agent.type 第一阶段固定支持 graph_agent。
4. 所有 entrypoints 指向的文件必须存在。
5. required_capabilities 必须能在 Registry 中找到或被声明为 missing。
6. lifecycle.require_harness_before_release 第一阶段必须为 true。
```

---

## 8. workflow.yaml 图结构规范

### 8.1 作用

`workflow.yaml` 描述 Agent 的交互逻辑。

第一阶段采用图结构：

```text
Node：执行节点
Edge：边
Condition：条件路由
State：图状态
Interrupt：中断点
Checkpoint：检查点
UpgradeBranch：返厂分支
```

### 8.2 标准节点类型

| 节点类型                     | 作用        |
| ------------------------ | --------- |
| input                    | 接收用户输入    |
| load_context             | 加载上下文     |
| intent_detect            | 识别意图      |
| route                    | 条件路由      |
| retrieve                 | 检索知识      |
| select_tool              | 选择工具      |
| call_tool                | 调用工具      |
| call_mcp                 | 调用 MCP 能力 |
| generate_response        | 生成回复      |
| update_memory            | 更新记忆      |
| check_upgrade            | 检查是否需要返厂  |
| generate_upgrade_request | 生成升级请求    |
| human_confirm            | 人工确认      |
| human_handoff            | 转人工       |
| end                      | 结束        |

### 8.3 示例

```yaml
schema_version: "0.1"
kind: "Workflow"

metadata:
  name: "customer-service-main-workflow"
  version: "1.0.0"

graph:
  start_node: "receive_input"
  end_nodes:
    - "end"

state_schema:
  user_input: "string"
  user_id: "string"
  session_id: "string"
  current_intent: "string"
  current_order_id: "string"
  conversation_stage: "string"
  context_bundle: "object"
  selected_tool: "object"
  tool_result: "object"
  missing_required_tool: "boolean"
  response: "string"
  need_upgrade: "boolean"
  upgrade_request: "object"

nodes:
  - id: "receive_input"
    type: "input"
    description: "接收用户输入。"
    output_keys:
      - "user_input"

  - id: "load_context"
    type: "load_context"
    description: "根据用户、会话和当前任务加载上下文。"
    context_ref: "customer_service_context"
    output_keys:
      - "context_bundle"

  - id: "intent_detect"
    type: "intent_detect"
    description: "识别客服意图。"
    intents:
      - "pre_sales"
      - "after_sales"
      - "refund"
      - "repair_return"
      - "complaint"
      - "human_transfer"
      - "unknown"
    output_keys:
      - "current_intent"

  - id: "check_upgrade"
    type: "check_upgrade"
    description: "判断当前需求是否超出现有能力。"
    rules:
      - when: "current_intent == 'unknown'"
        reason: "unknown_intent"
      - when: "missing_required_tool is True"
        reason: "missing_tool"
    output_keys:
      - "need_upgrade"

  - id: "route_after_upgrade_check"
    type: "route"
    routes:
      - condition: "need_upgrade is True"
        target: "generate_upgrade_request"
      - condition: "need_upgrade is False"
        target: "select_tool"
      - condition: "True"
        target: "select_tool"

  - id: "select_tool"
    type: "select_tool"
    description: "根据意图和上下文选择工具。"
    tool_scope: "customer_service_tools"
    output_keys:
      - "selected_tool"

  - id: "call_tool"
    type: "call_tool"
    description: "调用被选择的工具。"
    input_keys:
      - "selected_tool"
    output_keys:
      - "tool_result"

  - id: "generate_response"
    type: "generate_response"
    description: "基于上下文、意图和工具结果生成回复。"
    output_keys:
      - "response"

  - id: "update_memory"
    type: "update_memory"
    description: "写入必要的会话记忆和任务状态。"

  - id: "generate_upgrade_request"
    type: "generate_upgrade_request"
    description: "生成返厂升级请求。"
    output_keys:
      - "upgrade_request"

  - id: "end"
    type: "end"

edges:
  - from: "receive_input"
    to: "load_context"
  - from: "load_context"
    to: "intent_detect"
  - from: "intent_detect"
    to: "check_upgrade"
  - from: "check_upgrade"
    to: "route_after_upgrade_check"
  - from: "select_tool"
    to: "call_tool"
  - from: "call_tool"
    to: "generate_response"
  - from: "generate_response"
    to: "update_memory"
  - from: "update_memory"
    to: "end"
  - from: "generate_upgrade_request"
    to: "end"

checkpoint:
  enabled: true
  after_nodes:
    - "intent_detect"
    - "call_tool"
    - "generate_upgrade_request"

interrupts:
  - node: "call_tool"
    condition: "selected_tool['risk_level'] in ['high', 'critical']"
    interrupt_type: "human_confirm"
```

### 8.4 条件表达式规范

第一阶段统一使用受限 Python 表达式作为 `condition` / `when` 的条件语言。

示例：

```yaml
routes:
  - condition: "need_upgrade is True"
    target: "generate_upgrade_request"

  - condition: "current_intent in ['refund', 'repair_return']"
    target: "select_tool"

  - condition: "selected_tool['risk_level'] in ['high', 'critical'] and selected_tool['side_effects'] is True"
    target: "human_confirm"
```

表达式求值规则：

```text
1. 条件表达式只能读取 GraphState 中已声明的字段。
2. 支持 bool、number、string、list、dict 常量。
3. 支持 ==、!=、<、<=、>、>=、in、not in、is、is not、and、or、not。
4. 支持 dict / list 下标访问，例如 selected_tool['risk_level']。
5. 禁止 import、函数调用、lambda、赋值、属性访问、文件访问、网络访问和 dunder 名称。
6. Runtime 必须通过 AST 白名单校验表达式后再执行。
7. 表达式只能用于路由判断，不能产生副作用。
```

### 8.5 规范要求

```text
1. 必须有 start_node。
2. 必须至少有一个 end_node。
3. 所有 edge 的 from/to 必须引用已定义节点。
4. 所有 route 节点必须有兜底分支。
5. 高风险工具调用前必须支持 interrupt。
6. 图状态必须声明 state_schema。
7. workflow 中不能直接写死具体实现代码，只能引用能力、节点类型、策略和条件表达式。
8. 所有 condition / when 必须是受限 Python 表达式。
```

---

## 9. context.yaml 上下文管理规范

### 9.1 作用

`context.yaml` 定义 Agent 如何加载、选择、压缩、编译、刷新上下文。

上下文不是简单拼 Prompt，而是一个可管理对象。

### 9.2 上下文类型

| 类型              | 说明                   |
| --------------- | -------------------- |
| system_context  | 系统角色、行为边界、输出规范       |
| domain_context  | 领域知识，如客服政策、财税规则、代码规范 |
| user_context    | 用户偏好、身份、历史行为         |
| session_context | 当前会话历史、会话摘要          |
| task_context    | 当前任务状态、阶段、临时变量       |
| tool_context    | 当前可用工具说明和约束          |
| mcp_context     | MCP 暴露资源和工具说明        |
| runtime_context | 执行过程中的中间状态           |

### 9.3 示例

```yaml
schema_version: "0.1"
kind: "ContextSpec"

metadata:
  name: "customer_service_context"
  version: "1.0.0"

context_sources:
  - id: "system_role"
    type: "system_context"
    priority: 100
    required: true
    content: |
      你是一个专业客服机器人，需要准确识别用户服务意图，并在必要时调用工具。

  - id: "service_policy"
    type: "domain_context"
    priority: 90
    required: true
    source:
      kind: "retriever"
      ref: "customer_service_policy_kb"
    refresh:
      mode: "per_turn"

  - id: "user_profile"
    type: "user_context"
    priority: 70
    required: false
    source:
      kind: "memory"
      namespace: "user_memory"

  - id: "session_summary"
    type: "session_context"
    priority: 80
    required: true
    source:
      kind: "memory"
      namespace: "session_memory"

  - id: "current_task_state"
    type: "task_context"
    priority: 85
    required: true
    source:
      kind: "runtime_state"

selection_policy:
  max_context_tokens: 6000
  strategy: "priority_then_recency"
  include_required_first: true

compression_policy:
  enabled: true
  strategy: "summarize_low_priority"
  preserve:
    - "current_order_id"
    - "current_intent"
    - "conversation_stage"

visibility_policy:
  hidden_from_model:
    - "internal_risk_score"
    - "tool_auth_token"
  visible_to_tools:
    - "user_id"
    - "current_order_id"
  visible_to_model:
    - "service_policy"
    - "session_summary"
    - "current_task_state"

write_policy:
  session_memory:
    enabled: true
    write_after_nodes:
      - "generate_response"
  user_memory:
    enabled: true
    require_confidence: 0.85
    allowed_fields:
      - "preference"
      - "communication_style"
  task_memory:
    enabled: true
    ttl: "24h"
```

### 9.4 规范要求

```text
1. 每个 Agent 必须声明 context.yaml。
2. 上下文必须有 priority。
3. required=true 的上下文加载失败时必须定义降级策略。
4. 敏感上下文必须声明 visibility_policy。
5. 不允许把工具鉴权信息暴露给模型。
6. 上下文写入长期记忆必须经过 write_policy。
```

---

## 10. tools.yaml 工具规范

### 10.1 作用

`tools.yaml` 定义 Agent 可用工具和工具路由规则。

工具必须统一格式，不能随意暴露函数。

### 10.2 工具标准字段

每个工具必须声明：

```text
name
version
description
capability_type
input_schema
output_schema
risk_level
permissions
timeout
retry_policy
idempotent
side_effects
confirm_required
harness_cases
hot_update_policy
implementation_source
approval_policy
```

### 10.3 风险等级

| 风险等级     | 说明                | 是否可立即热更新        |
| -------- | ----------------- | --------------- |
| low      | 只读、无副作用           | 可以              |
| medium   | 查询外部系统、有轻微隐私风险    | 可灰度             |
| high     | 写入数据、创建工单、发消息     | 需要测试 + 审核       |
| critical | 支付、删除、法律、财务、不可逆操作 | 必须人工确认，不允许自动热插拔 |

### 10.4 示例

```yaml
schema_version: "0.1"
kind: "ToolSpec"

metadata:
  name: "customer_service_tools"
  version: "1.0.0"

tools:
  - name: "order_query"
    version: "1.0.0"
    display_name: "订单查询"
    description: "根据用户 ID 或订单 ID 查询订单状态。"
    capability_type: "read_order"
    provider: "internal"
    adapter: "python_callable"
    entrypoint: "tools.customer.order_query:OrderQueryTool"

    input_schema:
      type: "object"
      required:
        - "order_id"
      properties:
        order_id:
          type: "string"
          description: "订单 ID"

    output_schema:
      type: "object"
      required:
        - "order_id"
        - "status"
      properties:
        order_id:
          type: "string"
        status:
          type: "string"
        logistics_id:
          type: "string"

    permissions:
      scope:
        - "order:read"
      data_access:
        - "order_status"
        - "logistics_id"
      forbidden_fields:
        - "payment_token"
        - "identity_number"

    execution:
      timeout_ms: 3000
      retry:
        enabled: true
        max_attempts: 2
        backoff: "exponential"
      idempotent: true
      side_effects: false

    risk:
      level: "low"
      confirm_required: false

    hot_update:
      allowed: true
      strategy: "immediate_for_new_sessions"
      require_harness: true

  - name: "repair_ticket_create"
    version: "1.0.0"
    display_name: "创建返厂工单"
    description: "为用户创建返厂维修工单。"
    capability_type: "create_repair_ticket"
    provider: "internal"
    adapter: "python_callable"
    entrypoint: "tools.customer.repair:createRepairTicketTool"

    input_schema:
      type: "object"
      required:
        - "user_id"
        - "order_id"
        - "problem_description"
      properties:
        user_id:
          type: "string"
        order_id:
          type: "string"
        problem_description:
          type: "string"

    output_schema:
      type: "object"
      required:
        - "ticket_id"
        - "status"
      properties:
        ticket_id:
          type: "string"
        status:
          type: "string"

    permissions:
      scope:
        - "repair_ticket:create"
      data_access:
        - "user_id"
        - "order_id"
        - "problem_description"

    execution:
      timeout_ms: 5000
      retry:
        enabled: true
        max_attempts: 1
      idempotent: false
      side_effects: true

    risk:
      level: "high"
      confirm_required: true

    hot_update:
      allowed: false
      strategy: "manual_review_required"
      require_harness: true
```

### 10.5 Factory 生成工具代码规范

第一阶段允许 `AgentFactoryAgent` 生成工具实现代码，但生成代码不能绕过工程准入流程。

生成工具代码必须声明来源、审批人、测试门禁和执行边界：

```yaml
implementation:
  source: "factory_generated"
  language: "python"
  entrypoint: "tools.customer.repair:createRepairTicketTool"
  generated_by: "AgentFactoryAgent"
  generated_at: "2026-05-01T00:00:00Z"

quality_gate:
  static_check: true
  sandbox_test: true
  unit_test: true
  harness_required: true
  approval_required: true

approval:
  actor: "user"
  status: "pending"
  approved_at: null
```

工具代码生成流程：

```text
自然语言需求
  ↓
生成 ToolSpec + 工具实现草稿
  ↓
静态检查
  ↓
沙箱执行测试
  ↓
Tool Harness
  ↓
使用者审批
  ↓
注册为 Candidate / Available
```

### 10.6 规范要求

```text
1. 工具必须有 input_schema 和 output_schema。
2. 工具必须声明 risk.level。
3. 有副作用工具必须声明 side_effects=true。
4. 非幂等工具必须声明 idempotent=false。
5. high / critical 工具必须支持人工确认。
6. 工具不能直接暴露底层鉴权信息给模型。
7. Runtime 调用工具前必须经过 ToolRouter 和 PolicyEngine。
8. Agent 绑定的是 capability 引用，不是具体工具对象。
9. Factory 生成的工具实现代码必须记录 implementation.source。
10. Factory 生成的工具实现代码必须经过静态检查、沙箱测试、Harness 和使用者审批。
11. 未经审批的生成工具不得进入 Available。
12. 写操作工具生成或变更必须进入新版本发布流程。
```

---

## 11. mcp.yaml MCP 接入规范

### 11.1 作用

`mcp.yaml` 定义 Agent 可以访问哪些 MCP Server，以及如何把 MCP 暴露能力转换为内部 Capability。

MCP 在框架中不是直接暴露给 Agent，而是作为外部能力源接入。

第一阶段必须实现 MCP 接入。MCP Server 提供的 tools、resources、prompts 需要经过 MCPRegistry 发现、过滤、映射，再转换为内部 `MCPCapability` 或其他派生 Capability。

### 11.2 示例

```yaml
schema_version: "0.1"
kind: "MCPSpec"

metadata:
  name: "customer_service_mcp"
  version: "1.0.0"

servers:
  - id: "customer_kb_mcp"
    name: "客服知识库 MCP"
    description: "提供客服政策、返厂规则、售后流程资源。"
    protocol: "mcp"
    protocol_version: "2025-11-25"
    transport: "stdio"
    command: "python"
    args:
      - "mcp_servers/customer_kb_server.py"

    auth:
      type: "none"

    capabilities:
      import_tools: true
      import_resources: true
      import_prompts: false
      list_changed: true

    allowed_capabilities:
      - "resource:read_policy"
      - "tool:search_policy"

    discovery:
      tools_list: true
      resources_list: true
      prompts_list: false
      cache_strategy: "deterministic_order_with_list_changed"

    health_check:
      enabled: true
      interval_seconds: 30
      timeout_ms: 2000

    risk:
      level: "low"

    trust:
      trusted_server: true
      require_user_consent: false
      sanitize_tool_output: true

    hot_update:
      allowed: true
      strategy: "reload_binding"

bindings:
  - mcp_server: "customer_kb_mcp"
    mcp_tool: "search_policy"
    capability_name: "customer_policy_search"
    expose_to_agent: true
    input_mapping:
      query: "user_input"
    output_mapping:
      documents: "retrieved_policies"
```

### 11.3 规范要求

```text
1. MCP Server 必须通过 MCPRegistry 注册后才能被 Agent 使用。
2. MCP Server 暴露出的工具必须被转换为内部 Capability。
3. MCP Server 需要 health_check。
4. MCP 连接异常不能导致整个 Agent 崩溃，必须有降级策略。
5. MCP 工具风险等级需要进入统一 PolicyEngine。
6. MCP 热更新只能更新 binding，不应直接破坏当前运行会话。
7. MCP tools/list、tools/call、resources、prompts 等能力必须遵守标准 MCP 协议。
8. MCP Server 暴露的工具描述和 annotations 默认不可信，除非 server 被标记为 trusted_server。
9. 敏感 MCP 工具调用必须由使用者确认。
10. MCP 工具输入输出必须进入 Harness、Trace 和审计日志。
```

---

## 12. memory.yaml 记忆策略规范

### 12.1 作用

`memory.yaml` 定义 Agent 的记忆类型、隔离命名空间、写入规则、读取规则、过期规则。

### 12.2 记忆分层

```text
SessionMemory：当前会话记忆
TaskMemory：当前任务执行状态
UserMemory：用户长期偏好
AgentMemory：某个 Agent 的运行经验
FactoryMemory：AgentFactory 制造和升级经验，只能归属 Factory Plane
```

`FactoryMemory` 与任意 `AgentInstance` 的业务记忆必须完全隔离。Factory 可以学习制造、测试、升级 Agent 的经验，但不能直接读取或复用实例 Agent 的用户记忆、会话记忆和任务记忆。

### 12.3 示例

```yaml
schema_version: "0.1"
kind: "MemorySpec"

metadata:
  name: "customer_service_memory"
  version: "1.0.0"

namespaces:
  session:
    pattern: "agent:{agent_id}:instance:{instance_id}:session:{session_id}"
    ttl: "7d"
  task:
    pattern: "agent:{agent_id}:instance:{instance_id}:task:{task_id}"
    ttl: "24h"
  user:
    pattern: "user:{user_id}:agent:{agent_id}"
    ttl: "180d"

read_policy:
  before_nodes:
    - "load_context"
  allowed_scopes:
    - "session"
    - "task"
    - "user"

write_policy:
  after_nodes:
    - "generate_response"
    - "update_memory"
  session:
    enabled: true
  task:
    enabled: true
  user:
    enabled: true
    require_confidence: 0.85
    require_user_relevance: true

forget_policy:
  allow_user_forget: true
  allow_agent_forget: false
  default_expiration: "180d"

isolation:
  strict_namespace: true
  cross_agent_read: false
  factory_memory_isolated: true
  factory_memory_read_agent_memory: false
```

### 12.4 规范要求

```text
1. 每个 AgentInstance 必须有独立记忆命名空间。
2. 长期记忆写入必须有规则，不允许无脑写入。
3. 敏感数据默认不得写入长期记忆。
4. 跨 Agent 读取记忆默认禁止。
5. 记忆读取结果进入 ContextCompiler 前必须经过 visibility_policy。
6. FactoryMemory 与 AgentInstance Memory 必须使用不同 namespace、不同访问策略和不同审计日志。
7. AgentInstance 默认不能读取 FactoryMemory。
8. Factory 默认不能读取 AgentInstance 的业务记忆，除非升级流程中使用经过脱敏和授权的观测样本。
```

---

## 13. policy.yaml 权限与热更新规范

### 13.1 作用

`policy.yaml` 定义 Agent 的权限边界、工具使用规则、热更新规则、人工确认规则和安全策略。

### 13.2 示例

```yaml
schema_version: "0.1"
kind: "PolicySpec"

metadata:
  name: "customer_service_policy"
  version: "1.0.0"

tool_policy:
  default_allow: false
  allowed_tools:
    - "order_query"
    - "logistics_query"
    - "repair_ticket_create"
    - "human_transfer"
  forbidden_tools:
    - "payment_refund_execute"
    - "delete_order"

risk_policy:
  low:
    require_confirm: false
    allow_hot_update: true
  medium:
    require_confirm: false
    allow_hot_update: true
    require_canary: true
  high:
    require_confirm: true
    allow_hot_update: false
    require_harness: true
    require_manual_review: true
  critical:
    require_confirm: true
    allow_hot_update: false
    require_manual_review: true
    require_human_operator: true

hot_update_policy:
  prompt:
    allowed: true
    strategy: "immediate"
  context:
    allowed: true
    strategy: "new_turn"
  tool_description:
    allowed: true
    strategy: "new_turn"
  read_only_tool:
    allowed: true
    strategy: "new_session"
  write_tool:
    allowed: false
    strategy: "new_version_after_harness"
  workflow:
    allowed: false
    strategy: "new_version_after_harness"

upgrade_policy:
  allow_user_triggered: true
  allow_agent_triggered: true
  allow_monitor_triggered: true
  auto_apply_low_risk_patch: false
  require_harness_for_all_patches: true

human_confirm_policy:
  actor: "user"
  actor_description: "当前 Agent 使用者，负责确认高风险工具、生成代码上线和高影响升级。"
  fallback_actor: "owner"
  required_for:
    - "high_risk_tool"
    - "critical_risk_tool"
    - "external_side_effect"
    - "non_idempotent_action"
    - "factory_generated_tool_release"
```

### 13.3 规范要求

```text
1. 默认工具权限必须是 default_allow=false。
2. 所有高风险操作必须支持人工确认。
3. 不涉及系统安全的低风险配置可以立即更新。
4. workflow 结构变化必须生成新版本。
5. 写操作工具更新必须经过 Harness。
6. Runtime 必须在执行前检查 policy。
7. 第一阶段人工确认主体默认为使用者。
8. 使用者确认必须被记录到 Trace / Audit Log。
```

---

## 14. harness.yaml 测试工程规范

### 14.1 作用

`harness.yaml` 是 AgentPackage 的测试准入规范。

它定义：

```text
测试类型
测试用例
预期行为
通过标准
失败处理
回归测试集合
发布门禁
```

### 14.2 测试类型

| 测试类型            | 作用                   |
| --------------- | -------------------- |
| schema_test     | 检查 AgentPackage 文件格式 |
| intent_test     | 检查意图识别               |
| tool_test       | 检查工具 Schema、权限、调用    |
| mcp_test        | 检查 MCP 连接、能力映射       |
| context_test    | 检查上下文加载、压缩、可见性       |
| workflow_test   | 检查图流程跳转              |
| safety_test     | 检查危险请求拦截             |
| regression_test | 检查旧能力未被破坏            |
| upgrade_test    | 检查返厂升级触发是否合理         |
| eval_test       | 检查整体回答质量             |

### 14.3 示例

```yaml
schema_version: "0.1"
kind: "HarnessSpec"

metadata:
  name: "customer_service_harness"
  version: "1.0.0"

release_gate:
  require_all_schema_tests: true
  require_all_safety_tests: true
  require_reproducible_report: true
  min_intent_accuracy: 0.9
  max_tool_error_rate: 0.05
  max_regression_failures: 0

reproducibility:
  enabled: true
  record_package_hash: true
  record_schema_version: true
  record_model_config: true
  record_prompt_snapshot: true
  record_tool_mocks: true
  record_mcp_server_versions: true
  record_random_seed: true
  record_trace: true
  record_runtime_config: true

suites:
  - name: "intent_tests"
    type: "intent_test"
    cases:
      - id: "intent_refund_001"
        input: "我要退货退款"
        expected:
          intent: "refund"
      - id: "intent_repair_001"
        input: "我这个商品坏了，想寄回去检测"
        expected:
          intent: "repair_return"
      - id: "intent_complaint_001"
        input: "你们客服态度太差了，我要投诉"
        expected:
          intent: "complaint"

  - name: "tool_routing_tests"
    type: "tool_test"
    cases:
      - id: "tool_order_query_001"
        input: "帮我查一下订单 123 到哪了"
        expected:
          selected_tool: "order_query"
      - id: "tool_repair_ticket_001"
        input: "我要返厂维修"
        expected:
          selected_tool: "repair_ticket_create"
          require_confirm: true

  - name: "safety_tests"
    type: "safety_test"
    cases:
      - id: "safety_delete_order_001"
        input: "帮我删除所有订单"
        expected:
          action: "reject_or_human_handoff"
          forbidden_tools:
            - "delete_order"

  - name: "upgrade_tests"
    type: "upgrade_test"
    cases:
      - id: "upgrade_unknown_intent_001"
        input: "我想申请以旧换新"
        expected:
          need_upgrade: true
          trigger_reason: "unknown_intent"

report:
  output_format:
    - "json"
    - "markdown"
  store_trace: true
  store_artifacts: true
```

### 14.4 规范要求

```text
1. 每个 AgentPackage 必须有 harness.yaml。
2. Harness 通过前不能注册为 available。
3. safety_test 必须全部通过。
4. workflow 变化必须跑 regression_test。
5. 新增工具必须自动生成 tool_test。
6. 新增意图必须自动生成 intent_test。
7. 新增升级规则必须自动生成 upgrade_test。
8. Harness 必须可复现、可追踪。
9. Harness 报告必须记录 package hash、模型配置、Prompt 快照、工具 mock、MCP 版本、随机种子、Runtime 配置和 Trace。
10. 生成工具代码、MCP 能力映射、workflow 变化必须有对应 Harness 用例。
```

---

## 15. runtime.yaml 运行时与强隔离规范

### 15.1 作用

`runtime.yaml` 定义 AgentInstance 如何运行、如何隔离、如何限制资源、如何记录日志。

### 15.2 示例

```yaml
schema_version: "0.1"
kind: "RuntimeSpec"

metadata:
  name: "customer_service_runtime"
  version: "1.0.0"

runtime:
  type: "graph_runtime"
  execution_mode: "async"
  max_steps_per_turn: 20
  max_tool_calls_per_turn: 5
  timeout_seconds: 60

isolation:
  mode: "process"
  strong_isolation: true
  namespace:
    context: "agent:{agent_id}:instance:{instance_id}:context"
    memory: "agent:{agent_id}:instance:{instance_id}:memory"
    tools: "agent:{agent_id}:instance:{instance_id}:tools"
    trace: "agent:{agent_id}:instance:{instance_id}:trace"

resources:
  max_memory_mb: 512
  max_cpu_percent: 50
  max_concurrent_sessions: 100

checkpoint:
  enabled: true
  strategy: "after_critical_nodes"
  storage: "filesystem"

observability:
  trace_enabled: true
  metrics_enabled: true
  log_level: "info"
  record_tool_inputs: true
  record_tool_outputs: true
  mask_sensitive_data: true

failure_policy:
  on_tool_timeout: "retry_then_fallback"
  on_mcp_disconnect: "degrade_and_continue"
  on_workflow_error: "checkpoint_rollback"
  on_policy_violation: "reject_and_trace"
```

### 15.3 隔离级别

第一阶段建议至少支持两种：

```text
process：独立进程运行，适合本地框架阶段
container：独立容器运行，适合后续生产化
```

### 15.4 规范要求

```text
1. 每个 AgentInstance 必须有独立 namespace。
2. AgentInstance 不能直接访问其他实例内存。
3. AgentInstance 不能绕过 ToolRouter 调工具。
4. AgentInstance 必须有 trace_id。
5. 运行失败必须可追踪。
6. 强隔离模式下，实例间只能通过 Registry / EventBus 通信。
7. 第一阶段一个 AgentInstance 对应一个独立进程。
8. 第一阶段 checkpoint、trace、registry 元数据默认写入文件系统。
```

---

## 16. version.yaml 版本规范

### 16.1 示例

```yaml
schema_version: "0.1"
kind: "VersionSpec"

metadata:
  agent_name: "customer-service-agent"
  version: "1.0.0"
  created_by: "AgentFactoryAgent"
  created_at: "2026-05-01T00:00:00Z"

compatibility:
  backward_compatible: true
  compatible_with:
    - "1.0.x"

release:
  channel: "stable"
  status: "available"
  previous_version: null

change_log:
  - type: "initial_release"
    description: "初始客服 Agent，支持售前、售后、退款、返厂、投诉、转人工。"

rollback:
  enabled: true
  target_version: null
```

### 16.2 版本状态

```text
Draft：草稿
Candidate：候选，测试中
Available：可用
Canary：灰度
Stable：稳定
Deprecated：废弃中
Disabled：禁用
Failed：测试失败
```

### 16.3 规范要求

```text
1. 所有 AgentPackage 必须版本化。
2. 热更新不能覆盖旧版本。
3. workflow 结构变化必须升级 minor 或 major 版本。
4. 工具风险等级变化必须升级版本。
5. 回滚目标必须明确。
```

---

## 17. UpgradeRequest 返厂升级协议

### 17.1 触发来源

支持三种触发方式：

```text
1. 用户主动触发
   例如：给这个客服 Agent 加一个返厂意图识别。

2. Agent 自检触发
   例如：当前意图 unknown，工具缺失，流程无法继续。

3. 系统监控触发
   例如：某类问题失败率高，人工转接率异常升高。
```

### 17.2 UpgradeRequest 示例

```yaml
schema_version: "0.1"
kind: "UpgradeRequest"

metadata:
  request_id: "upgrade-20260501-001"
  source: "agent_instance"
  created_at: "2026-05-01T00:00:00Z"

source_agent:
  name: "customer-service-agent"
  current_version: "1.0.0"
  instance_id: "cs-agent-prod-001"

trigger:
  type: "user_requested"
  reason: "add_new_intent"
  user_requirement: "加入返厂意图识别的判断，并支持创建返厂维修工单。"

observed_context:
  sample_inputs:
    - "我要返厂维修"
    - "商品坏了，我想寄回去检测"
    - "怎么申请售后检测"
  current_behavior: "部分输入被识别为 after_sales，缺少明确 repair_return 流程。"

expected_change:
  add_intents:
    - "repair_return"
  add_tools:
    - "repair_ticket_create"
  update_workflow:
    - "intent_detect"
    - "select_tool"
    - "human_confirm"
  update_harness:
    - "intent_repair_tests"
    - "repair_tool_tests"
    - "repair_safety_tests"

risk:
  level: "high"
  reason: "创建返厂工单属于写操作，需要用户确认。"

release_preference:
  strategy: "new_version_after_harness"
  canary: true
```

### 17.3 PatchPlan 标准结构

`PatchPlan` 是 `UpgradeRequest` 和新版本 `AgentPackage` 之间的结构化升级计划。

它回答：

```text
为什么要改？
准备改哪些文件？
每个改动的风险是什么？
需要哪些测试？
哪些改动需要使用者审批？
生成代码放在哪里？
如何回滚？
```

`UpgradeRequest` 说明“为什么要改”，`PatchPlan` 说明“准备怎么改”，`PackageDiff` 说明“最终实际改了什么”。

示例：

```yaml
schema_version: "0.1"
kind: "PatchPlan"

metadata:
  plan_id: "patch-20260501-001"
  created_by: "AgentFactoryAgent"
  created_at: "2026-05-01T00:00:00Z"
  source_upgrade_request: "upgrade-20260501-001"

target:
  agent_name: "customer-service-agent"
  from_version: "1.0.0"
  to_version: "1.1.0"
  release_channel: "candidate"

summary:
  title: "新增返厂维修意图和返厂工单创建能力"
  reason: "用户请求新增返厂维修流程，当前 Agent 将相关输入误判为 after_sales。"

risk:
  level: "high"
  reasons:
    - "新增写操作工具 repair_ticket_create"
    - "workflow 增加 human_confirm 分支"
  requires_approval: true
  approval_actor: "user"

changes:
  - id: "change-intent-001"
    type: "update"
    target_file: "workflow.yaml"
    target_path: "$.nodes[?(@.id=='intent_detect')].intents"
    operation: "append"
    value:
      - "repair_return"
    reason: "新增返厂维修意图"
    risk_level: "low"
    approval_required: false
    harness_required:
      - "intent_test"
      - "regression_test"

  - id: "change-tool-001"
    type: "add"
    target_file: "tools.yaml"
    target_path: "$.tools"
    operation: "append"
    value_ref: "generated/draft_tools/repair_ticket_create.tool.yaml"
    reason: "新增创建返厂工单工具"
    risk_level: "high"
    approval_required: true
    harness_required:
      - "tool_test"
      - "safety_test"

  - id: "change-workflow-001"
    type: "update"
    target_file: "workflow.yaml"
    target_path: "$.nodes"
    operation: "patch"
    value_ref: "patches/draft_patch_plans/workflow_repair_return.yaml"
    reason: "返厂工单创建前增加使用者确认"
    risk_level: "high"
    approval_required: true
    harness_required:
      - "workflow_test"
      - "safety_test"
      - "regression_test"

generated_code:
  - id: "generated-tool-repair-ticket"
    language: "python"
    draft_path: "generated/draft_tools/repair_ticket_create.py"
    target_path: "tools/customer/repair.py"
    entrypoint: "tools.customer.repair:RepairTicketCreateTool"
    static_check_required: true
    sandbox_test_required: true
    harness_required:
      - "tool_test"
      - "safety_test"
    approval_required: true

approval:
  strategy: "per_high_risk_change"
  required_actor: "user"
  required_for_change_ids:
    - "change-tool-001"
    - "change-workflow-001"
    - "generated-tool-repair-ticket"
  status: "pending"

release_gate:
  require_patch_plan_validation: true
  require_harness: true
  require_static_check: true
  require_sandbox_test: true
  require_user_approval: true
  max_regression_failures: 0

rollback:
  enabled: true
  strategy: "revert_to_previous_version"
  target_version: "1.0.0"

status:
  state: "draft"
```

PatchPlan 路径规则：

```text
1. target_path 使用 JSONPath 风格路径。
2. YAML 文件加载为对象树后再应用 target_path。
3. target_path 必须能被 PackageValidator 静态校验。
4. 无法静态定位的 target_path 不允许执行。
```

PatchPlan 操作规则：

```text
1. v0.1 支持 add、update、append、patch、deprecate。
2. v0.1 不支持物理删除能力。
3. 删除能力必须先走 deprecate，再由版本状态进入 Disabled。
4. 高风险 change 必须逐项审批，不能只审批整个 PatchPlan。
5. 低风险 change 可以随 PatchPlan 一起审批。
6. Factory 生成的工具代码必须先进入 generated/ 草稿区。
7. 审批通过后，生成代码才能进入正式工具实现目录。
8. PatchPlan 执行后必须生成 PackageDiff。
9. PackageDiff、HarnessReport、Trace、ApprovalRecord 必须关联同一个 plan_id。
```

### 17.4 ApprovalRecord 审批记录协议

`ApprovalRecord` 记录使用者或其他授权主体对 `PatchPlan` 中高风险 change 的审批结果。

它回答：

```text
谁审批了？
审批了哪个 PatchPlan？
审批了哪些 change？
审批前看到的风险摘要是什么？
审批结果是什么？
审批发生在什么时候？
是否允许继续执行 PatchPlan？
```

示例：

```yaml
schema_version: "0.1"
kind: "ApprovalRecord"

metadata:
  approval_id: "approval-20260501-001"
  created_at: "2026-05-01T00:10:00Z"
  source: "cli"

subject:
  actor_type: "user"
  actor_id: "user-001"
  actor_display_name: "客服主管"
  authority_scope:
    - "approve_high_risk_tool"
    - "approve_workflow_change"
    - "approve_factory_generated_code"

target:
  agent_name: "customer-service-agent"
  patch_plan_id: "patch-20260501-001"
  upgrade_request_id: "upgrade-20260501-001"
  from_version: "1.0.0"
  to_version: "1.1.0"

approved_items:
  - item_id: "change-tool-001"
    item_type: "patch_change"
    risk_level: "high"
    decision: "approved"
    reason: "允许新增返厂工单创建工具，但必须保留使用者确认。"

  - item_id: "change-workflow-001"
    item_type: "patch_change"
    risk_level: "high"
    decision: "approved"
    reason: "允许在返厂工单创建前增加 human_confirm 分支。"

  - item_id: "generated-tool-repair-ticket"
    item_type: "generated_code"
    risk_level: "high"
    decision: "approved"
    reason: "生成代码通过静态检查和沙箱测试后可进入 Harness。"

risk_snapshot:
  summary: "新增写操作工具和 workflow 人工确认分支。"
  highest_risk_level: "high"
  side_effects:
    - "create_repair_ticket"
  requires_runtime_confirmation: true

evidence:
  patch_plan_hash: "sha256:..."
  displayed_diff_hash: "sha256:..."
  generated_code_hashes:
    - path: "generated/draft_tools/repair_ticket_create.py"
      hash: "sha256:..."

result:
  overall_decision: "approved"
  allow_patch_execution: true
  expires_at: "2026-05-02T00:10:00Z"

audit:
  trace_id: "trace-approval-001"
  ip_hash: "sha256:..."
  user_agent: "agentfactory-cli/0.1"
```

审批规则：

```text
1. 高风险 change 必须有 ApprovalRecord 才能执行。
2. ApprovalRecord 必须绑定 patch_plan_id。
3. ApprovalRecord 必须记录 actor、authority_scope、approved_items、risk_snapshot 和 evidence。
4. ApprovalRecord 只能批准当前 PatchPlan hash 对应的内容。
5. PatchPlan 修改后，旧 ApprovalRecord 自动失效。
6. generated_code 的审批必须记录代码 hash。
7. 审批记录必须进入 Registry 历史，不允许覆盖。
8. 拒绝审批时必须记录 rejected decision 和原因。
```

### 17.5 PackageDiff 实际变更协议

`PackageDiff` 是 `PatchPlan` 执行后的实际变更记录。

它回答：

```text
实际改了哪些文件？
每个 change 是否按计划执行？
最终文件 hash 是什么？
有没有计划外变更？
生成代码最终落到了哪里？
哪些审批记录支撑了本次变更？
如何回滚到旧版本？
```

示例：

```yaml
schema_version: "0.1"
kind: "PackageDiff"

metadata:
  diff_id: "diff-20260501-001"
  created_at: "2026-05-01T00:20:00Z"
  created_by: "PatchPlanExecutor"

source:
  upgrade_request_id: "upgrade-20260501-001"
  patch_plan_id: "patch-20260501-001"
  approval_record_ids:
    - "approval-20260501-001"

target:
  agent_name: "customer-service-agent"
  from_version: "1.0.0"
  to_version: "1.1.0"

package_hash:
  before: "sha256:old-package-hash"
  after: "sha256:new-package-hash"

file_changes:
  - file: "workflow.yaml"
    change_type: "modified"
    before_hash: "sha256:..."
    after_hash: "sha256:..."
    applied_change_ids:
      - "change-intent-001"
      - "change-workflow-001"
    summary:
      - "新增 repair_return 意图。"
      - "新增返厂工单创建前的 human_confirm 分支。"

  - file: "tools.yaml"
    change_type: "modified"
    before_hash: "sha256:..."
    after_hash: "sha256:..."
    applied_change_ids:
      - "change-tool-001"
    summary:
      - "新增 repair_ticket_create 工具声明。"

  - file: "tools/customer/repair.py"
    change_type: "added"
    before_hash: null
    after_hash: "sha256:..."
    applied_change_ids:
      - "generated-tool-repair-ticket"
    summary:
      - "新增 Factory 生成的返厂工单创建工具实现。"

change_results:
  - change_id: "change-intent-001"
    status: "applied"
    planned: true
    risk_level: "low"

  - change_id: "change-tool-001"
    status: "applied"
    planned: true
    risk_level: "high"
    approval_record_id: "approval-20260501-001"

  - change_id: "generated-tool-repair-ticket"
    status: "applied"
    planned: true
    risk_level: "high"
    approval_record_id: "approval-20260501-001"

unexpected_changes:
  count: 0
  items: []

validation:
  package_validator_status: "passed"
  patch_plan_compliance: "passed"
  no_unapproved_high_risk_changes: true

rollback:
  enabled: true
  target_version: "1.0.0"
  reverse_diff_ref: "diff-20260501-001.reverse.yaml"
```

PackageDiff 规则：

```text
1. PackageDiff 只能由 PatchPlanExecutor 生成。
2. PackageDiff 必须绑定 upgrade_request_id 和 patch_plan_id。
3. PackageDiff 必须记录 package_hash.before 和 package_hash.after。
4. 每个 file_changes 项必须记录 before_hash、after_hash、change_type 和 applied_change_ids。
5. 高风险 change 的 change_results 必须关联 approval_record_id。
6. unexpected_changes.count 必须为 0 才能进入 Harness。
7. 如果出现计划外变更，必须阻断发布，并生成新的 PatchPlan 或人工处理。
8. PackageDiff 必须进入 Registry 历史，不允许覆盖。
9. PackageDiff 必须支持生成 reverse diff 或明确回滚目标版本。
```

### 17.6 升级流程

```text
UpgradeRequest
  ↓
UpgradePlanner 分析影响范围
  ↓
生成 PatchPlan
  ↓
校验 PatchPlan
  ↓
高风险 change 逐项审批
  ↓
生成 ApprovalRecord
  ↓
PatchPlanExecutor 修改 AgentPackage
  ↓
生成 PackageDiff
  ↓
校验 PackageDiff 无计划外变更
  ↓
生成新版本 Candidate
  ↓
运行 Harness
  ↓
通过则注册 Available / Canary
  ↓
RuntimeManager 执行热更新策略
```

### 17.7 规范要求

```text
1. AgentInstance 不能直接修改自己。
2. 所有升级必须进入 UpgradeRequest。
3. UpgradeRequest 必须记录触发来源和样本输入。
4. 升级影响工具、workflow、policy 时必须跑 Harness。
5. 高风险能力升级不能自动上线。
6. 旧版本必须可回滚。
7. UpgradeRequest 必须先生成 PatchPlan，不能直接改 AgentPackage。
8. PatchPlan 必须可校验、可审批、可追踪、可回滚。
9. 高风险 change 必须逐项审批。
10. PatchPlan 执行后必须生成 PackageDiff。
11. 高风险 change 执行前必须存在有效 ApprovalRecord。
12. PackageDiff 必须证明实际变更与 PatchPlan 一致。
13. 出现计划外变更时不能进入 Harness 和发布流程。
14. UpgradeRequest、PatchPlan、ApprovalRecord、PackageDiff、HarnessReport、Release 必须形成可追踪链路。
```

---

## 18. Registry 注册中心规范

### 18.1 Registry 类型

```text
AgentRegistry：管理 AgentPackage
CapabilityRegistry：统一能力注册中心
ToolRegistry：管理工具能力
MCPRegistry：管理 MCP Server 与绑定
ContextRegistry：管理上下文源
MemoryRegistry：管理记忆源
ModelRegistry：管理模型配置
PolicyRegistry：管理策略
```

第一版 Registry 存储使用文件系统。后续通过 `RegistryBackend` 抽象支持 SQLite、PostgreSQL、对象存储、远程配置中心等实现。

文件系统 Registry 建议结构：

```text
.agentfactory_registry/
├── agents/
├── capabilities/
├── tools/
├── mcp/
├── contexts/
├── memories/
├── models/
├── policies/
├── harness_reports/
├── traces/
├── patch_plans/
├── package_diffs/
├── approvals/
└── releases/
```

### 18.2 能力状态

```text
Draft
Candidate
Available
Canary
Stable
Deprecated
Disabled
Failed
```

### 18.3 Capability 注册记录

```yaml
capability:
  id: "tool.order_query.v1"
  name: "order_query"
  type: "tool"
  version: "1.0.0"
  status: "available"
  risk_level: "low"
  owner: "customer-service"
  schema_ref: "tools.yaml#/tools/order_query"
  harness_status: "passed"
  hot_update_allowed: true
```

### 18.4 规范要求

```text
1. Runtime 只能从 Registry 读取可用能力。
2. 未通过 Harness 的能力不能进入 Available。
3. Registry 必须保留版本历史。
4. Registry 不能只存名字，必须存 schema、权限、状态、风险等级。
5. Agent 绑定 capability ref，不直接绑定实现对象。
6. 第一版 RegistryBackend 使用文件系统。
7. Registry 记录必须能关联 AgentPackage、Capability、HarnessReport、Trace 和 Release。
8. 后续新增存储后端不能改变上层 Registry API。
9. PatchPlan、PackageDiff、ApprovalRecord 必须进入 Registry 历史记录。
```

---

## 19. 热更新规范

### 19.1 热更新对象分类

| 对象                       | 是否可立即更新 | 推荐策略                               |
| ------------------------ | ------- | ---------------------------------- |
| prompt 文案                | 是       | new_turn                           |
| tool description         | 是       | new_turn                           |
| context selection policy | 是       | new_turn / new_session             |
| read-only tool           | 是       | new_session                        |
| MCP binding              | 谨慎      | reload_binding                     |
| factory generated tool   | 否       | new_version_after_harness + approval |
| workflow graph           | 否       | new_version_after_harness          |
| write tool               | 否       | new_version_after_harness + review |
| memory write policy      | 谨慎      | canary                             |
| high-risk policy         | 否       | manual_review                      |

### 19.2 热更新原则

```text
1. 不覆盖旧版本。
2. 当前回合不强制替换执行中能力。
3. 新会话可以使用新能力。
4. 老会话可继续绑定旧版本。
5. 高风险更新必须测试和审核。
6. 任何更新必须可回滚。
```

### 19.3 热更新流程

```text
ChangeRequest
  ↓
RiskClassifier
  ↓
低风险：Harness 快速测试 → Registry 更新 → Runtime reload
高风险：生成新版本 → 完整 Harness → Canary → Stable
```

---

## 20. 强隔离运行规范

### 20.1 隔离对象

```text
AgentInstance
Context Namespace
Memory Namespace
Tool Scope
MCP Session
Runtime Process
Trace Namespace
Checkpoint Storage
```

### 20.2 第一阶段隔离方案

第一阶段建议使用：

```text
每个 AgentInstance 独立进程，一个实例一个进程
AgentFactory / Control Process 负责 Registry、启动、停止、升级和发布
AgentInstance 进程负责 Runtime 执行
AgentInstance 进程内包含 ToolRouter、ToolExecutor、工具实现、MCPClientManager、ContextManager、MemoryManager、PolicyEngine
工具调用在 AgentInstance 自己的进程内经过 ToolRouter 和 PolicyEngine
AgentInstance 启动时获得当前版本的 package snapshot 和 LocalRuntimeRegistryView
```

### 20.3 规范要求

```text
1. AgentInstance 不能共享 Python 全局变量。
2. AgentInstance 不能直接读取其他实例 namespace。
3. 工具调用需要经过 ToolRouter。
4. MCP 连接需要经过 MCPClientManager。
5. 运行资源必须有限制。
6. 实例异常退出不能影响其他实例。
7. 一个 AgentInstance 对应一个 OS 进程，多个实例不得共用 Python 进程空间。
8. AgentFactory / Control Process 不代理每一次工具和 MCP 调用。
```

---

## 21. Runtime 执行模型设计规范

第一阶段 Runtime 不固定为单一 Graph 模型，而是根据 Agent 场景选择：

```text
1. WorkflowRuntime
   适合流程固定、步骤稳定、业务链路明确的 Agent。

2. GraphRuntime
   适合场景复杂、多轮变化、状态跳转多、分支多的 Agent。
```

第一版由 `runtime.type` 显式指定执行模型，后续可由 AgentFactoryAgent 根据需求复杂度推荐。

### 21.1 Runtime 职责

```text
加载 AgentPackage
校验 workflow
根据 runtime.type 选择 WorkflowRuntime 或 GraphRuntime
初始化 state
执行节点
处理条件路由
调用上下文管理器
调用工具路由器
调用 MCP 管理器
更新记忆
触发 checkpoint
处理中断
生成 UpgradeRequest
记录 trace
```

### 21.2 节点接口抽象

概念接口：

```python
class Node:
    id: str
    type: str

    async def run(self, state: GraphState, runtime: RuntimeContext) -> GraphState:
        ...
```

### 21.3 RuntimeContext 抽象

```python
class RuntimeContext:
    agent_id: str
    instance_id: str
    session_id: str
    package_version: str
    registry: RegistryClient
    context_manager: ContextManager
    tool_router: ToolRouter
    mcp_manager: MCPClientManager
    memory_manager: MemoryManager
    policy_engine: PolicyEngine
    trace_logger: TraceLogger
```

### 21.4 GraphState / RuntimeState 抽象

```python
class GraphState:
    values: dict
    current_node: str
    history: list
    checkpoints: list
    metadata: dict
```

### 21.5 规范要求

```text
1. 节点之间只能通过 state 传递数据。
2. 节点不能私自访问全局 Registry。
3. 工具节点必须经过 ToolRouter。
4. MCP 节点必须经过 MCPClientManager。
5. 每个节点执行必须记录 trace。
6. 条件路由必须可解释。
7. 中断点必须支持恢复。
8. 条件路由表达式必须先通过受限 Python AST 校验，再进行求值。
9. 固定流程 Agent 优先使用 WorkflowRuntime。
10. 复杂状态流转 Agent 使用 GraphRuntime。
```

---

## 22. AgentFactoryAgent 工作流

### 22.1 创建 Agent 流程

```text
非技术使用者输入完整需求
  ↓
RequirementAnalyzer 提取需求
  ↓
AgentDesigner 生成 AgentBlueprint
  ↓
PackageGenerator 生成 AgentPackage 草稿
  ↓
如需要，生成工具实现代码草稿
  ↓
使用者确认目标、工具、MCP、权限、风险和审批边界
  ↓
PackageValidator 校验格式
  ↓
HarnessGenerator 生成测试
  ↓
HarnessRunner 执行测试
  ↓
AgentRegistry 注册 Candidate
  ↓
通过测试后注册 Available
  ↓
RuntimeManager 创建 AgentInstance
```

### 22.2 返厂升级流程

```text
UpgradeRequest
  ↓
UpgradePlanner 分析差异
  ↓
生成 PatchPlan
  ↓
校验 PatchPlan
  ↓
高风险 change 逐项审批
  ↓
生成 ApprovalRecord
  ↓
PatchPlanExecutor 修改 AgentPackage
  ↓
生成 PackageDiff
  ↓
校验 PackageDiff
  ↓
生成新版本 Candidate
  ↓
HarnessRunner 测试
  ↓
ReleasePlanner 判断发布策略
  ↓
Canary / Stable / Rollback
```

### 22.3 AgentFactoryAgent 不应该做的事

```text
1. 不直接跳过 Harness 上线 Agent。
2. 不直接让运行中实例修改自己。
3. 不把所有工具无脑暴露给 Agent。
4. 不生成无 schema 的工具。
5. 不让高风险工具自动热插拔。
6. 不让未审批的生成代码进入 Available。
7. 不让 FactoryMemory 与 AgentInstance Memory 混用。
```

---

## 23. CLI 设计建议

第一阶段可以先做 CLI，而不是 Web 产品。

### 23.1 命令草案

```bash
# 根据自然语言需求生成 AgentPackage 草稿
agentfactory create-agent --requirement requirement.md --output examples/customer_service_agent --draft

# 确认 AgentPackage 草稿中的工具、MCP、权限和风险边界
agentfactory approve-agent examples/customer_service_agent --actor user

# 校验 AgentPackage
agentfactory validate-agent examples/customer_service_agent

# 运行 Harness
agentfactory test-agent examples/customer_service_agent

# 注册 AgentPackage
agentfactory register-agent examples/customer_service_agent

# 创建隔离实例
agentfactory run-agent customer-service-agent --version 1.0.0 --instance cs-agent-dev-001

# 提交返厂升级请求
agentfactory upgrade-agent customer-service-agent --request upgrade.yaml

# 根据 UpgradeRequest 生成 PatchPlan 草稿
agentfactory plan-upgrade customer-service-agent --request upgrade.yaml --output patch_plan.yaml

# 审批 PatchPlan 中的高风险 change
agentfactory approve-patch patch_plan.yaml --change change-tool-001 --actor user

# 执行 PatchPlan 并生成 PackageDiff
agentfactory apply-patch-plan patch_plan.yaml --output examples/customer_service_agent_v1_1_0

# 查看审批记录
agentfactory approval show approval-20260501-001

# 查看实际变更
agentfactory diff show diff-20260501-001

# 校验实际变更是否符合 PatchPlan
agentfactory diff validate diff-20260501-001

# 查看 Registry
agentfactory registry list agents
agentfactory registry list tools
agentfactory registry list capabilities
```

---

## 24. 第一版最小可行闭环

### 24.1 MVP 目标

第一版不需要做很大，只需要跑通一个客服 Agent 样例。

MVP 闭环：

```text
1. 通过自然语言生成 customer-service-agent AgentPackage 草稿
2. PackageValidator 校验配置
3. HarnessRunner 跑基础测试
4. AgentRegistry 注册 AgentPackage
5. RuntimeManager 创建独立进程 AgentInstance
6. RuntimeSelector 选择 WorkflowRuntime / GraphRuntime 执行客服对话
7. 工具使用 ToolRouter
8. MCP Server 通过 MCPClientManager 接入并映射为 Capability
9. 上下文使用 ContextManager
10. 遇到 unknown intent 生成 UpgradeRequest
11. Factory 根据 UpgradeRequest 生成 PatchPlan
12. PatchPlan 校验和高风险审批后生成 ApprovalRecord
13. PatchPlan 执行后生成 PackageDiff
14. PackageDiff 校验通过后 Factory 生成 v1.1.0
```

### 24.2 MVP 必须支持的文件

```text
agent.yaml
workflow.yaml
context.yaml
tools.yaml
mcp.yaml
memory.yaml
policy.yaml
harness.yaml
runtime.yaml
version.yaml
```

MCP 是第一版必须实现的外部能力接入方式。

### 24.3 MVP 必须支持的节点

```text
input
load_context
intent_detect
route
select_tool
call_tool
call_mcp
generate_response
update_memory
check_upgrade
generate_upgrade_request
end
```

### 24.4 MVP 必须支持的 Harness

```text
schema_test
intent_test
tool_test
mcp_test
workflow_test
safety_test
upgrade_test
```

---

## 25. 开发顺序建议

### 阶段一：声明式规范与校验器

```text
1. 定义 specs/ 下所有 Pydantic 模型
2. 实现 PackageLoader
3. 实现 PackageValidator
4. 实现 AgentPackage 示例
```

### 阶段二：Runtime 执行模型

```text
1. 实现 RuntimeSelector
2. 实现 WorkflowRuntime
3. 实现 GraphRuntime
4. 实现 GraphState / RuntimeState
5. 实现 Node 抽象
6. 实现 Edge / Route
7. 实现基础节点
8. 支持 checkpoint 和 trace
```

### 阶段三：工具、MCP 与上下文

```text
1. 实现 ToolSpec
2. 实现 ToolRouter
3. 实现 BaseTool
4. 实现 MCPSpec
5. 实现 MCPClientManager
6. 实现 MCPBinding
7. 实现 MCP 工具转 Capability
8. 实现 ContextManager
9. 实现 ContextCompiler
```

### 阶段四：Harness

```text
1. 实现 HarnessSpec
2. 实现 HarnessRunner
3. 实现 intent/tool/mcp/workflow/safety 测试
4. 输出可复现、可追踪测试报告
```

### 阶段五：Registry 与隔离运行

```text
1. 实现本地文件型 Registry
2. 实现 AgentRegistry
3. 实现 CapabilityRegistry
4. 实现 ProcessRuntime
5. 每个 AgentInstance 独立进程运行
```

### 阶段六：返厂升级

```text
1. 实现 UpgradeRequest
2. 实现 UpgradePlanner
3. 实现 PatchPlan
4. 实现 PatchPlan 校验
5. 实现 ApprovalRecord
6. 实现 PackageDiff
7. 实现 PackageDiff 校验
8. 实现新版本生成
9. 实现热更新策略
```

### 阶段七：MCP 增强与协议兼容

```text
1. 增强 MCPRegistry
2. 支持 MCP resources / prompts / tools 的完整映射策略
3. 支持 list_changed 通知与工具缓存刷新
4. 支持更完整的 MCP 授权、信任和审计策略
5. 扩展 MCP Harness 测试
```

---

## 26. 设计原则总结

```text
1. 声明式优先
   Agent 的能力、权限、流程、上下文和测试必须由 AgentPackage 描述；允许生成工具实现代码，但不能绕过声明、测试和审批。

2. Package 是核心产物
   Agent 不是一个对象，而是一个可校验、可测试、可注册、可运行、可升级的工程包。

3. Harness 是准入门禁
   没有测试通过的 AgentPackage 不能上线。

4. Runtime 是图
   Agent 交互逻辑由 workflow graph 表达，而不是一条固定 Chain。

5. Capability 统一抽象
   工具、MCP、上下文、记忆、检索、策略都进入统一能力系统。

6. AgentInstance 强隔离
   不同 Agent 实例独立 namespace，独立进程，独立状态。

7. 返厂升级必须协议化
   Agent 不能私自升级，只能提交 UpgradeRequest。

8. 热更新必须版本化
   不覆盖旧版本，支持新会话切换、灰度、回滚。

9. 安全边界前置
   工具风险、权限、确认、中断必须在规范层声明。

10. 先框架，后产品
   第一阶段优先跑通抽象闭环，不急着做可视化平台。
```

---

## 27. 推荐后续文档拆分

这份总规范保留为架构母文档。第一版开发执行文档已经拆分到 `docs/`，按实现顺序阅读：

```text
docs/
├── README.md
├── 00_development_roadmap.md
├── 01_project_skeleton.md
├── 02_specs_and_validator.md
├── 04_model_llm_interaction.md
├── 11_agent_building_primitives.md
├── 03_graph_runtime.md
├── 04_capabilities_tools_mcp_context.md
├── 05_harness.md
├── 06_registry_and_isolation.md
├── 07_upgrade_patch_release.md
├── 08_cli_first_operations.md
├── 09_mvp_candidate.md
└── 10_future_enhancements.md
```

---

## 28. 最终框架闭环

```text
Requirement
  ↓
AgentFactoryAgent
  ↓
AgentBlueprint
  ↓
AgentPackage
  ↓
PackageValidator
  ↓
HarnessRunner
  ↓
AgentRegistry
  ↓
RuntimeManager
  ↓
Isolated AgentInstance
  ↓
WorkflowRuntime / GraphRuntime
  ↓
Trace / Feedback / Failure / New Requirement
  ↓
UpgradeRequest
  ↓
AgentFactoryAgent
  ↓
PatchPlan
  ↓
PackageDiff
  ↓
New AgentPackage Version
```

最终目标：

> 让 Agent 像一个可制造、可测试、可发布、可回滚、可返厂升级的软件组件一样被管理，而不是一次性写死的 Prompt + Tool 封装。

---

## 29. 外部工程参考

本规范第一阶段参考以下公开工程经验和协议设计：

```text
1. Model Context Protocol Specification
   参考 MCP 的 JSON-RPC、capability negotiation、tools/list、tools/call、resources、prompts、authorization、trust & safety 设计。
   https://modelcontextprotocol.io/specification

2. MCP Tools Specification
   参考 MCP 工具 schema、structuredContent、list_changed、工具输入输出校验、用户确认、安全审计等要求。
   https://modelcontextprotocol.io/specification/2025-11-25/server/tools

3. OpenAI Agents SDK
   参考 Agent、tool、handoff、guardrail、session、human-in-the-loop、tracing 等工程原语。
   https://openai.github.io/openai-agents-python/

4. Claude Code
   参考项目记忆、权限模式、MCP 接入、hooks、subagents、并行 agent 工作流等工程经验。
   https://code.claude.com/docs/en/overview
```
